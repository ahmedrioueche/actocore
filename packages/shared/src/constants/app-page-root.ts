/** Default Root container page seeded when a project has no app layout pages. */
export const DEFAULT_ROOT_PAGE_SLUG = 'root';

export const DEFAULT_ROOT_PAGE_TITLE = 'Root';

export const DEFAULT_ROOT_PAGE_ROUTE = '/';

export const DEFAULT_ROOT_PAGE_DESCRIPTION =
  'Product map root. Groups all screens on the layout graph. Not a user-facing page — set hostContext.currentPage to a child screen slug.';
