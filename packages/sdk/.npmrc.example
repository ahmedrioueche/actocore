# Optional — only if you publish/install from GitHub Packages instead of public npm.
# Consumers on public npm do not need any .npmrc.
#
# @ahmedrioueche:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
